name: TARS Automate Service Generator

on:
  push:
    branches:
      - main
    paths:
      - 'ENDR.yaml'
      - 'SVCS.yaml'
      - 'TARS/**'
      - 'TARS/TARS.py'
      - '.github/workflows/tars-init.yml'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  tars-reconcile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: TARS Init
        run: |
          python -m pip install --upgrade pip
          python -m pip install fastapi pydantic pyyaml jinja2 certifi

      - name: SVCS Check
        id: svcs_check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 TARS/TARS.py svcs-check \
            --repo-root . \
            --open-pr

      - name: Summary
        run: |
          echo "Changes detected: ${{ steps.svcs_check.outputs.has_changes }}"
          echo "Changed services: ${{ steps.svcs_check.outputs.changed_services }}"
          echo "Removed services: ${{ steps.svcs_check.outputs.removed_services }}"
          echo "Changed files: ${{ steps.svcs_check.outputs.changed_file_count }}"
          echo "Deleted files: ${{ steps.svcs_check.outputs.delete_file_count }}"
          if [ -n "${{ steps.svcs_check.outputs.pr_url }}" ]; then
            echo "PR: ${{ steps.svcs_check.outputs.pr_url }}"
          else
            echo "No PR opened"
          fi

      - name: Upload reconcile state
        uses: actions/upload-artifact@v4
        with:
          name: tars-svcs-state-${{ github.run_id }}
          path: ${{ steps.svcs_check.outputs.state_file }}
          if-no-files-found: error
