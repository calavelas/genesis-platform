apiVersion: apps/v1
kind: Deployment
metadata:
  name: endr-svc-demo
  labels:
    app.kubernetes.io/name: endr-svc-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: endr-svc-demo
  template:
    metadata:
      labels:
        app.kubernetes.io/name: endr-svc-demo
    spec:
      containers:
        - name: endr-svc-demo
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
          resources:
{{ toYaml .Values.resources | indent 12 }}
