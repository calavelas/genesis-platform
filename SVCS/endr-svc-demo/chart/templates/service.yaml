apiVersion: v1
kind: Service
metadata:
  name: endr-svc-demo
spec:
  selector:
    app.kubernetes.io/name: endr-svc-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
