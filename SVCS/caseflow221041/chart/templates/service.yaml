apiVersion: v1
kind: Service
metadata:
  name: caseflow221041
spec:
  selector:
    app.kubernetes.io/name: caseflow221041
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
