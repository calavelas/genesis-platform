apiVersion: v1
kind: Service
metadata:
  name: hello-service
spec:
  selector:
    app.kubernetes.io/name: hello-service
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
