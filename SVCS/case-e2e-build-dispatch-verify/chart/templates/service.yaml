apiVersion: v1
kind: Service
metadata:
  name: case-e2e-build-dispatch-verify
spec:
  selector:
    app.kubernetes.io/name: case-e2e-build-dispatch-verify
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
