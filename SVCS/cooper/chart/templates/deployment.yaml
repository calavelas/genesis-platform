apiVersion: apps/v1
kind: Deployment
metadata:
  name: cooper
  labels:
    app.kubernetes.io/name: cooper
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cooper
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cooper
    spec:
      containers:
        - name: cooper
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
          resources:
{{ toYaml .Values.resources | indent 12 }}
