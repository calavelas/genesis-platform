# Template: python-fastapi

Service template files use Jinja-style placeholders and will be rendered by the IDP API.

Generated app behavior:
- `GET /` returns a simple HTML landing page with the service name.
- `GET /healthz` returns a JSON health response.
